package homeworks.hw1;

/**
 * A simple enum used by {@link MyStack#MyStack(int, StackSize)} to determine the inner representation of
 * {@link MyStack} instances. For your homework, you should try to figure out why this approach is selected.
 *
 * @see MyStack
 *
 *
 */
public enum StackSize {
    SMALL, LARGE
}
